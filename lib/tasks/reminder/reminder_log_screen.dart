import 'package:flutter/material.dart';

class ReminderLogScreen extends StatelessWidget {
  const ReminderLogScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.amber,
    );
  }
}